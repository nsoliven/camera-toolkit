# Artwork

- `AppIcon.png` is the selected source artwork used to build every size in the macOS `.icns` file.
- `GitHubSocialPreview.jpg` is the same artwork encoded below GitHub's 1 MB upload limit for **Settings → General → Social preview**.

The packaging script generates `AppIcon.icns` from the PNG. Do not edit the generated `.icns` directly.
